# libs — code Python dùng chung

- `common/` — config (đọc env), logging, JWT utils
- `messaging/` — wrapper RabbitMQ (publish/consume)
- `llm/` — LLM adapter (Claude/OpenAI) + prompt templates
- `schemas/` — Pydantic models dùng chung (Job, ProfileData, GeneratedCV, ATSReport)
