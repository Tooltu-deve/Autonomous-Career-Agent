\documentclass[a4paper,10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[margin=0.75in]{geometry}
\usepackage[hidelinks]{hyperref}
\usepackage{enumitem}
\usepackage{titlesec}

\pagestyle{empty}
\raggedbottom
\raggedright

% Section heading: chữ hoa + gạch ngang bên dưới (classic style)
\titleformat{\section}
  {\vspace{10pt}\large\bfseries\uppercase}
  {}{0em}{}
  [\vspace{2pt}\hrule height 0.8pt \vspace{8pt}]

\setlist[itemize]{leftmargin=1.2em, itemsep=2pt, parsep=0pt, topsep=2pt}

\begin{document}

% ---------- HEADER ----------
\begin{center}
\BLOCK{ if hdr.full_name }{\Huge \textbf{\VAR{hdr.full_name | tex}}}\\[4pt]\BLOCK{ endif }%
\BLOCK{ if hdr.headline }{\large \VAR{hdr.headline | tex}}\\[8pt]\BLOCK{ endif }%
\small
\BLOCK{ if hdr.location }\VAR{hdr.location | tex}\\[4pt]\BLOCK{ endif }%
% Contact căn cột: 2 cột (trái/phải) ngăn bằng đường dọc, các \textbf căn phải
% cột trái, căn trái cột phải -> dấu | và nhãn thẳng hàng giữa 2 dòng.
\BLOCK{ if hdr.email or hdr.phone or hdr.github_url or hdr.linkedin_url }
\begin{tabular}{r@{~~\textbar~~}l}
\BLOCK{ if hdr.email or hdr.phone }\textbf{Email:}~\VAR{hdr.email | tex} & \textbf{Phone:}~\VAR{hdr.phone | tex} \\[2pt]\BLOCK{ endif }%
\BLOCK{ if hdr.github_url or hdr.linkedin_url }\textbf{GitHub:}~\VAR{hdr.github_url | tex} & \textbf{LinkedIn:}~\VAR{hdr.linkedin_url | tex}\BLOCK{ endif }%
\end{tabular}
\BLOCK{ endif }
\end{center}
\vspace{-2pt}

% ---------- SUMMARY ----------
\section{Objective}
\VAR{cv.summary | tex}

% ---------- WORK EXPERIENCE ----------
\BLOCK{ if cv.experience }
\section{Work Experience}
\BLOCK{ for e in cv.experience }
\textbf{\VAR{e.organization | tex}} \hfill \BLOCK{ if e.start_date }\VAR{e.start_date | tex} -- \VAR{(e.end_date or "Present") | tex}\BLOCK{ endif }\par
\textbf{\VAR{e.title | tex}}\par
\BLOCK{ if e.description }\VAR{e.description | tex}\par\BLOCK{ endif }
\vspace{8pt}
\BLOCK{ endfor }
\BLOCK{ endif }

% ---------- EDUCATION ----------
\BLOCK{ if cv.education }
\section{Education}
\BLOCK{ for ed in cv.education }
\textbf{\VAR{ed.school | tex}} \hfill \BLOCK{ if ed.start_date }\VAR{ed.start_date | tex} -- \VAR{(ed.end_date or "Present") | tex}\BLOCK{ endif }\par
\BLOCK{ if ed.degree }\VAR{ed.degree | tex}\BLOCK{ if ed.field_of_study } --- \VAR{ed.field_of_study | tex}\BLOCK{ endif }\par\BLOCK{ endif }%
\BLOCK{ if ed.description }\VAR{ed.description | tex}\par\BLOCK{ endif }
\vspace{6pt}
\BLOCK{ endfor }
\BLOCK{ endif }

% ---------- CERTIFICATIONS ----------
\BLOCK{ if cv.certifications }
\section{Certifications}
\BLOCK{ for cert in cv.certifications }
\textbf{\VAR{cert.title | tex}} \hfill \VAR{cert.obtain_date | tex}\par
\vspace{4pt}
\BLOCK{ endfor }
\BLOCK{ endif }

% ---------- SKILLS ----------
\BLOCK{ if cv.skills }
\section{Skills}
\VAR{ (cv.skills | map("tex") | join(", ")) }
\BLOCK{ endif }

\end{document}
